# Cost Control

> **Superseded by [`cost-model.md`](cost-model.md).**

The per-resource cost model — why each resource is required, its minimum viable configuration, its cost category (FREE / LOW-COST / VARIABLE / POTENTIALLY EXPENSIVE), whether it can be stopped, whether it bills continuously, and how it is removed at teardown — is in [`cost-model.md`](cost-model.md), with every claim cited to an official AWS pricing page.

Operating discipline is in [`../operations/lab-runbook.md`](../operations/lab-runbook.md) and [`../operations/maintenance.md`](../operations/maintenance.md).

Kept as a pointer rather than deleted, so links from earlier commits still resolve.
